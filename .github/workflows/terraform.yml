name: Terraform

on:
  push:
    branches: [main]
    paths:
      - "infra/terraform/**"
      - ".github/workflows/terraform.yml"
  pull_request:
    paths:
      - "infra/terraform/**"
      - ".github/workflows/terraform.yml"
  workflow_dispatch:

env:
  TF_WORKING_DIR: infra/terraform

jobs:
  terraform:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    defaults:
      run:
        working-directory: ${{ env.TF_WORKING_DIR }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.8

      # Check committed .tf only — before generated prod.tfvars exists.
      - name: Terraform Format Check
        run: terraform fmt -check -recursive

      - name: Write tfvars
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          cat > environments/prod.tfvars <<EOF
          project_id        = "${{ secrets.GCP_PROJECT_ID }}"
          region            = "${{ vars.GCP_REGION || 'europe-west2' }}"
          environment       = "prod"
          github_repository = "${{ github.repository }}"
          nextauth_url      = "${{ vars.NEXTAUTH_URL }}"
          app_base_url      = "${{ vars.NEXTAUTH_URL }}"
          EOF
          if [ -n "$CRON_SECRET" ]; then
            printf 'cron_secret = "%s"\n' "$CRON_SECRET" >> environments/prod.tfvars
          fi
          terraform fmt environments/prod.tfvars

      - name: Terraform Init
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="prefix=prod"

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan -var-file=environments/prod.tfvars -no-color -out=tfplan

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: terraform apply -auto-approve tfplan
